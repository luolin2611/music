<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.music.core.dao.sys_user.SysUserDao" >
    <!-- 查询用户 -->
    <select id="findSysUser" parameterType="String" resultType="sysUser">
	    select *
	    from sys_user
	    where user_name = #{user_name}
	    and password =#{password}
	    and status = '00'
    </select>
    
    <select id="findSysUserByUsername" parameterType="String" resultType="sysUser">
	    select *
	    from sys_user
	    where user_name = #{user_name}
    </select>
    
    <insert id="insertSysUser" parameterType="sysUser">
	    insert into sys_user(
	                     user_id,
	                     user_name,
	                     password,
	                     update_time,
	                     status,
	                     user_level,
	                     real_name,
	                     add_user_name
	             )
	             values(
	             	    #{user_id},
	                    #{user_name},
	                    #{password},
	                    #{update_time},
	                    #{status},
	                    #{user_level},
	                    #{real_name},
	                    #{add_user_name}
	            )
	</insert>
	
	<resultMap id="sysUserList" type="sysUser">
        <result column="user_id" property="user_id" />
        <result column="user_name" property="user_name" />
        <result column="password" property="password" />
        <result column="update_time" property="update_time" />
        <result column="status" property="status" />
        <result column="user_level" property="user_level" />
        <result column="real_name" property="real_name" />
        <result column="add_user_name" property="add_user_name" />
    </resultMap>
	
	 <select id="findAllSysUser" resultMap="sysUserList">
	    select * from sys_user order by update_time DESC
    </select>
    
     <select id="findSysUserByIdAndPass" parameterType="String" resultType="String">
	    select user_id
	    from sys_user
	    where user_id = #{user_id} and password = #{password}
    </select>
    
    <update id="updateSysUserPass" parameterType="String">
    		update sys_user set password = #{password} where user_id = #{user_id}
    </update>
    
    
    
</mapper>
